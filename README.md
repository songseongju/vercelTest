# vercelTest

정적 페이지.

- `index.html` — **STAR DASH · 별빛 질주**. 캔버스 기반 미니 슈팅/회피 아케이드 게임.
- `letter.html` — 봉투를 눌러 여는 편지 (`/letter`).
- `photos/` — `01.webp` ~ `55.webp`.

빌드 단계 없음. 루트를 그대로 서빙합니다.

## 게임 조작

- 드래그(마우스 / 터치)로 이동, 방향키 또는 WASD도 사용 가능
- `P` 또는 `Esc` 일시정지, `Space` 시작·일시정지
- 별 +100점, 최고 점수는 `localStorage`에 저장
